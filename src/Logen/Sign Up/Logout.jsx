import { useContext } from "react";
import { Athcontes } from "../../Provadar/AthoProvadar";


const Logout = () => {
    const { logout, user } = useContext(Athcontes)
    
    return (
        <div>
            
        </div>
    );
};

export default Logout;